%%% -*- mode: erlang -*-
%%% $Id$
%%%

{application, gproc,
 [
  {description, "GPROC"},
  {vsn, git},
  {id, "GPROC"},
  {registered, [ ] },
  %% NOTE: do not list applications which are load-only!
  {applications, [ kernel, stdlib ] },
  {modules, [
    gproc,
    gproc_app,
    gproc_dist,
    gproc_init,
    gproc_lib,
    gproc_sup
   ]
  },
  {mod, {gproc_app, []} }
 ]
}.
